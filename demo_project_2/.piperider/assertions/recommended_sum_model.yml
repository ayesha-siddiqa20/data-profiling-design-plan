# Auto-generated by Piperider based on table "sum_model"
sum_model:  # Table Name
  # Test Cases for Table
  tests:
  - metric: row_count
    assert:
      gte: 0
    tags:
    - RECOMMENDED
  columns:
    total_price:  # Column Name
      # Test Cases for Column
      tests:
      - name: assert_column_schema_type
        assert:
          schema_type: FLOAT
        tags:
        - RECOMMENDED
