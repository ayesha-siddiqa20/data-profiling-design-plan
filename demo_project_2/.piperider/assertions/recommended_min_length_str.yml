# Auto-generated by Piperider based on table "min_length_str"
min_length_str:  # Table Name
  # Test Cases for Table
  tests:
  - metric: row_count
    assert:
      gte: 0
    tags:
    - RECOMMENDED
  columns:
    length:  # Column Name
      # Test Cases for Column
      tests:
      - name: assert_column_schema_type
        assert:
          schema_type: INTEGER
        tags:
        - RECOMMENDED
