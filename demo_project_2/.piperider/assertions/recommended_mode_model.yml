# Auto-generated by Piperider based on table "mode_model"
mode_model:  # Table Name
  # Test Cases for Table
  tests:
  - metric: row_count
    assert:
      gte: 0
    tags:
    - RECOMMENDED
  columns:
    mode_value:  # Column Name
      # Test Cases for Column
      tests:
      - name: assert_column_schema_type
        assert:
          schema_type: VARCHAR
        tags:
        - RECOMMENDED
      - name: assert_column_unique
        tags:
        - RECOMMENDED
