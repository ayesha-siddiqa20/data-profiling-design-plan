# Auto-generated by Piperider based on table "skewness_model"
skewness_model:  # Table Name
  # Test Cases for Table
  tests:
  - metric: row_count
    assert:
      gte: 0
    tags:
    - RECOMMENDED
  columns:
    pearson_coeff:  # Column Name
      # Test Cases for Column
      tests:
      - name: assert_column_schema_type
        assert:
          schema_type: FLOAT
        tags:
        - RECOMMENDED
      - name: assert_column_value
        assert:
          lte: 0.0591
        tags:
        - RECOMMENDED
